{
  "kind": "Service",
  "apiVersion": "v1",
  "metadata": {
    "name": "example-service"
  },
  "spec": {
    "ports": [{
                "port": 8080,
                "targetPort": 8888
              }],
    "selector": {
      "app": "nomApp"
    },
    "type": "NodePort"
  }
}